## XRPL EVM Testnet (Hardhat deploy)
# Copy this file to `.env` locally and fill in values.
#
# IMPORTANT: never commit `.env` and never paste private keys into chat.

# RPC endpoint for XRPL EVM Testnet
XRPL_EVM_TESTNET_RPC_URL=https://rpc.testnet.xrplevm.org

# Deployer EVM private key (hex, 0x-prefixed). Keep this secret.
DEPLOYER_PRIVATE_KEY=0xYOUR_PRIVATE_KEY_HERE

## Optional minting (runs right after deploy)
# If unset, defaults to deployer address.
LXUSD_MINT_TO=0xYOUR_METAMASK_ADDRESS_HERE

# Human amount, NOT wei (e.g. "1000" mints 1000.0 tokens with 18 decimals)
LXUSD_MINT_AMOUNT=1000




